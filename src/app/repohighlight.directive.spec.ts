import { RepohighlightDirective } from './repohighlight.directive';

describe('RepohighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new RepohighlightDirective();
    expect(directive).toBeTruthy();
  });
});
